[pytest]
testpaths = tests
# Agrega el directorio backend/ al sys.path para que "import app.*" funcione
# al correr pytest desde backend/ (necesario en CI; en el contenedor ya estaba en el path).
pythonpath = .
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
